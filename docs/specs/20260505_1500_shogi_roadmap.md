# 将棋大会アプリ ロードマップ

**対象**: shogi_v4（沼津支部月例将棋大会管理）  
**最終更新**: 2026-05-13 v25
**ステータス**: **A-4 系完了 + Layout Safety L-3/L-4 完了 + A-5.1 保存安全化 SAVE-001 / 002 / 003 / 004 / 003b-1 / 003b-2 完了、大会運営回る状態**（A-4.3 / A-4.5 / A-4.6 main マージ済、A-4.4 は revert 済、Layout Safety L-3/L-4 で iPhone 375px レイアウト破綻を構造的に検出可能に。A-5.1-SAVE 系で `removePlayer` / `addPlayer` / 大会進行 core path 4 関数 / 参加者追加経路 4 callsite / 対局画面編集経路 3 callsite に「保存未確認検知」を導入、SAVE-004 で `generatePairing` の verify を field-compare に強化。基盤強化フェーズ「A-T 後継」として DevSecOps v2.0 Slim v1.1 配置 + Postmortem v2 確立）

---

## マクロロードマップ（Codex作成）

| Phase | テーマ | 状態 |
|-------|--------|------|
| 0 | 安定運用の土台（CI・手順書整備） | 🔜 |
| 1 | 実運用フィードバック収集 | 🔜 |
| 2 | 入力補助・報告書まわり | 🔜 |
| 3 | 閲覧・共有（読み取り専用） | 🔜 |
| 4 | 多人数入力（サーバー化・認証） | 🔜 |

---

## 実装フェーズ（Phase A系）

| Phase | 内容 | 状態 | 備考 |
|-------|------|------|------|
| A-1 | マスタデータ層・ワンクリック呼び出し・マイグレ・最小マスタタブ | ✅ 完了 | |
| A-2 | ふりがな3点セット（入力・検索・50音タブ） | ✅ 完了 | |
| A-2.5 | マイグレ貼り付け方式・JSON用語撤廃 | ✅ 完了 | |
| A-2.6 | iPhone Safari UX バグ修正 | ✅ 完了 | commit b34b1d0 |
| A-3 | マスタ管理画面フル機能 ＋ 登録画面×マスタ接続 | ✅ 完了 | PR#6マージ済み（commit 9929ac4） |
| A-4 | 実機運用フィードバック対応（最大版6項目・揺れ修正除く） | ✅ 完了・mainマージ済み（PR#8、Codex A-/Go） | Stage 1〜5 + 揺れ調査・1px e2e |
| A-4.2 | 過去参加者パネル/サジェストにA/Bクラスボタン | ✅ 完了・mainマージ済み（PR#9、Codex B+→trailing whitespace修正後Yes、ChatGPT v2 A判定） | 単体595/e2e272全件緑（A-4.2 リグレッション混入、A-T で構造的に検出予定） |
| **A-T** | **UI テスト基盤強化（Test Hardening）** | ✅ **区切り完了**（Stage 1/2a/2b/2c/4/6 完了、PR #11〜#17、3 層 regression 防止層構築：L1 構造的 e2e + L2 Mutation 監視 + L3 Visual regression。全 e2e 465、Mutation Score 65%（主因経路 100%）、13 visual baseline、production code 0 行改修） | A-4.1 / A-4.2.1 / A-7 すべての前に置く独立フェーズ。完了レポート: `docs/specs/20260507_1720_shogi_at_phase_completion_report_v1.md`。残 Stage 3/5/7 はリリース時 or 別契機で再判断 |
| A-4.2.1 | A-4.2 hotfix（pp-add-btn を mousedown + touchstart 化） | ❌ **revert 済**（PR #20、commit `be270cb`） | event handler 戦略変更は的外れ、真因は別レイヤー（過去参加者パネルで既登録者の A/B ボタンが silent fail）→ A-4.3 で根因対応 |
| A-4.3 | 過去参加者からのクラス変更機能 + マスタ `last_class` 表示 | ✅ **完了・main マージ済**（PR #21、commit `ba76473`） | `changePlayerClass` 純粋関数追加、3 ケース確認ダイアログ（未登録/別クラス/同クラス）、F7 編集モーダルで `last_class` 編集可、マスタ一覧に「前回クラス」カラム追加 |
| A-4.4 | 過去参加者パネル UX 改善（2 セクション分離、行内「現在:Xクラス」テキスト追加） | ❌ **revert 済**（PR #22 → revert、commit `0ad3099` → `a66f418`） | iPhone 375px で氏名縦書き化 P0 障害（行内要素追加で flex 破綻）、Codex Devil's Advocate §1 が外れたケース。教訓は A-4.5 / Layout Safety L-3/L-4 へ反映 |
| A-4.5 | 過去参加者パネル 3 セクション分離 + 行 2 段レイアウト + F7 簡素化 + bug fix #7 | ✅ **完了・main マージ済**（PR #23、commit `cf7a970`） | A-4.4 失敗の構造的再発防止: 3 セクション（A クラス済 / B クラス済 / 未エントリー）、行 2 段レイアウトで氏名縦書き化解消、mobile-375 project の visual regression 新設（playwright.config.js で testIgnore 分離） |
| A-4.6 | エントリー済の現クラスボタンに色強調復活 | ✅ **完了・main マージ済**（PR #24、commit `d5f28ff`） | A-4.4 の轍を踏まずボタン style のみ変更（行構造・行内要素は A-4.5 と完全同一）、A 済→#bbdefb / B 済→#ffe0b2、`pp-add-btn-active` クラス復活、`pp-add-btn-highlight`（前回参加クラス強調）は撤廃のまま維持 |
| **Layout Safety L-3 + L-4** | **Overflow 検出 + Layout assertion ヘルパー（DevSecOps v2.0 §3.1 Layout Safety 実装）** | ✅ **完了・main マージ済**（PR #25、commit `43d5f7d`、production code **0 行改修**） | `test/helpers/layout-assertions.js` 新規（`expectNoHorizontalOverflow` / `expectHeightInRange` / `expectLeftOf` の 3 関数）+ 単体 15 件 + a4_5/a4_6 spec への適用。e2e 579 緑、汎用設計（将来 monorepo 化時に切り出し可能） |
| A-4.1 | スマホレイアウト揺れ修正（iPhone 16 Plus実機） | ✅ **Layout Safety L-3/L-4 で解消** | 髙橋さん実機確認済、行 2 段レイアウト + Overflow 検出 + Layout assertion で構造的に検出可能に |
| A-5 | iOS Safari印刷警告対応 | ❌ **スキップ判断**（2026-05-08） | 印刷機能不要のため永続スキップ |
| **A-5.1-SAVE-DESIGN-001** | 保存処理棚卸し v0.1（27 callsite + 6 I/O 系の整理、適用順序設計） | ✅ **完了・main マージ済**（PR #45） | `docs/notes/20260512_shogi_a5_1_save_design_001_persistence_inventory_v0.md`。MASTER-001 の「保存後 re-read で検証」原則を段階適用するための前提整備 |
| **A-5.1-SAVE-001** | 参加者削除時の保存未確認検知（`removePlayer`） | ✅ **完了・main マージ済**（PR #46、commit `219d328`） | 新規 helper `verifyPlayerAbsent` 追加。「保存失敗」と断定せず「保存未確認」として `showMsg(.., 'warn')` + `console.warn`。alert / rollback なし |
| **A-5.1-SAVE-002** | 参加者追加時の保存未確認検知（`addPlayer`） | ✅ **完了・main マージ済**（PR #47、commit `a19e193`） | 新規 helper `verifyPlayerPersistedById` 追加（id + cls + name の 3 軸一致）。同姓同名・A/B クラス境界での偽陽性を抑制 |
| **A-5.1-SAVE-003** | 大会進行 core path の保存未確認検知（`startTournament` / `generatePairing` / `setWinner` / `submitRound`） | ✅ **完了・main マージ済**（PR #48、commit `1e13ce1`） | 新規 helper `readPersistedState`（schema 検証付き）+ `pairingsMatchSnapshot`（field-compare）。SAVE-DESIGN-001 §5 の旧計画から「大会進行 core path Must 4 関数」に再定義し、会員マスタ系・参加者操作系は後続 SAVE-003b / SAVE-UX 等に切り出し |
| **A-5.1-SAVE-003b-1** | 参加者追加経路の保存未確認検知（サジェスト経由 / 過去参加者単発 / 過去参加者一括） | ✅ **完了・main マージ済**（PR #52、commit `af2f173`） | `handleSuggestClassAdd` / `handlePastParticipantClassAdd`（追加 + クラス変更）/ `finalizeAddPastParticipants`。既存 `verifyPlayerPersistedById` 流用、新 helper 追加なし。`finalizeAddPastParticipants` は warn 1 回集約。Codex A / Go |
| **A-5.1-SAVE-003b-2** | 対局画面編集経路の保存未確認検知（対戦相手変更 / swap / 過去結果修正 p1 / p2） | ✅ **完了・main マージ済**（PR #54、commit `9f4144f`） | `bindChangePairingModalEvents` / `bindEditPastResultModalEvents`。`readPersistedState` + `pairingsMatchSnapshot` 流用、トップレベル新 helper 追加なし。`bindEditPastResultModalEvents` はローカルクロージャ `verifyPastResultPersisted_ep(expectedWinner)` で多次元 index 欠落も検知。Codex A / Go |
| A-5.1-SAVE-003b-3 | 手動編集系の保存未確認検知（Should） | 🔲 未着手 | `updateField` / `bulkEditNames`。SAVE-UX の warn 集約方針合意が前提 |
| **A-5.1-SAVE-004** | `generatePairing` の簡易シグネチャ比較 | ✅ **完了・main マージ済**（PR #50、commit `42e4673`） | SAVE-003 では length-only。`pairingsMatchSnapshot` 流用で field-compare 化（SAVE-003 Codex Nice-to-Have 由来）。同件数 stale pairings を検知可能に。Codex Review A / Go（Must / Should / Nice-to-Have すべてなし） |
| A-5.1-SAVE-UX | warn 集約 / retry UI / 文言短縮 | 🔲 未着手 | SAVE-DESIGN-001 §1.4「後続タスクに切り出す UI 仕様」に整合。連続失敗時の閾値表示など |
| A-6 | マスタ画面説明文の古いニュアンス更新（Codex Minor #3） | 🔲 後回し可 | 軽微、運営に支障なし |
| — | A-3 e2e動作確認テスト | ✅ 完了 | PR#7マージ済み（commit bdd1e65）、80件全緑 |

---

## A-3 スコープ（確定）

### ★ 登録画面×マスタ接続（最優先・受付軽減の核心）

**狙い**：受付時のボトルネックは幹事による参加者情報の手入力。月例大会は常連が多いため、過去参加者をマスタから即呼び出せれば入力負荷を大幅に削減できる。

**確定仕様（2026-05-05）**：

| 項目 | 内容 |
|------|------|
| 候補表示方式 | A案：氏名入力欄にサジェスト表示（インライン） |
| 引き継ぎ項目 | 氏名・クラス・支部員区分・中学生以下区分（全フィールド） |
| 引き継ぎ後の修正 | 追加前に全フィールド変更可能 |
| クラス変更の対応 | 削除→再登録（1大会1〜2名の稀なケースのため複雑な仕組みは作らない） |

**確定の根拠**：
- 参加者は最大20名規模 → サジェストで苗字2〜3文字で絞れる、一覧不要
- クラス変更はほぼなし（成長期の子供で年1〜2名程度）
- 削除ボタンは既存UIに存在するため再登録フローは実現可能

**見送り**：
- B案（50音タブ一覧から選ぶ）→ 20名規模には過剰、タップ回数が増える

### マスタ管理画面フル機能
- F7 マスタ管理画面（編集・削除/tombstone操作・タブ内検索）
- F8 エクスポート/インポート
- クイックフィルタ（前回参加者・3ヶ月以内・常連）
- マイグレ対話UI
- 利用目的表示の詳細化

---



---

## 基盤強化フェーズ（A-T 後継）

A-T で確立した「3 層 regression 防止層構築」の延長線として、A-4 系の障害(特に A-4.4 P0 障害 → revert)を契機に確立した基盤強化群。

| 項目 | 状態 | 内容 |
|---|---|---|
| **DevSecOps v2.0 Slim v1.1** | ✅ 配置済（commit `ebe6675`、Slim で凍結方針） | 「4 系統 Safety 宣言」(Layout / UX / Operational / Assumption) + 16 項目（L-1〜L-7 / U-1〜U-3 / O-1〜O-3 / A-1〜A-3）。Full 版作成は当面凍結。`docs/specs/20260508_0025_devsecops_v2_4_safety_slim_draft_v1_1.md` |
| **Postmortem v2** | ✅ 確立（commit `524fece`） | A-4 系 5 障害の 5 Whys + 直接原因 / 構造的根因 2 段分析 + 共通パターン 4 個（A: 実機の見た目を見ていない / B: ユーザー視点が仕様書に含まれない / C: Claude.ai の動的状態把握不完全 / D: 既存正常状態への過信）+ 16 再発防止策。常駐文書 `docs/specs/_postmortem_a4_series.md` |
| **Layout Safety L-1〜L-4** | ✅ 完了 | L-1（mobile/desktop 両 VRT、A-4.5 で実装）/ L-2（bounding box 実測 = 縦書き化検出、A-4.5 で実装）/ L-3（Overflow 検出、L-3/L-4 mini Sprint で実装）/ L-4（Layout assertion ヘルパー、L-3/L-4 mini Sprint で実装）|
| Layout Safety L-5/L-6/L-7 | 🔲 将来必要時 | L-5（height/width 比併用）/ L-6（WebKit + iPhone device project）/ L-7（maxDiffPixelRatio 厳格化、現状 0.05）。実機 NG 発生時 or 大規模変更前に追加 |

---

## A-4 スコープ（v1仕様書ベース）

### 実機運用テストで判明した5項目

| # | 項目 | 概要 |
|---|------|------|
| 1 | 登録画面ふりがな入力欄＋IME自動取得 | CompositionEventでふりがな自動入力、手動編集可 |
| 2 | マスタ一覧に区分列追加 | member/grade を一覧表示（読み取り専用） |
| 3 | マスタ編集モーダルに区分追加 | name + yomi に加えて member/grade も編集可能 |
| 4 | 削除済みmember復元UI | tombstoneを表示・復元できる |
| 5 | ふりがな未入力者可視化 | サマリー・バッジ・フィルタで未入力を可視化 |

**スコープ外**：ふりがな自動「推定」（kuromoji.js）、ローマ字検索、異体字統合、半角カナ対応

**Stage構成**：6ステージ（UIのみ→IME自動取得→マスタ拡張→復元UI→未入力可視化→全体テスト）

## バックログ（将来オプション・現時点では着手しない）

### 基盤強化系（DevSecOps v2.0 / Layout Safety 周辺）

- **Layout Safety L-5/L-6/L-7**: height/width 比併用 / WebKit + iPhone device project / maxDiffPixelRatio 厳格化（現状 0.05 → 0.01）。実機 NG 発生時 or 大規模変更前に追加。
- **`_spec_template.md` 新設**: 仕様書テンプレート（§0 ユーザーストーリー / §6 Devil's Advocate 必須項目化）。常駐文書として `_business_model.md` / `_postmortem_a4_series.md` と並べる。
- **`_operational_rules.md` 新設**: Operational + Assumption Safety の運用ルール文書。実機確認 = Definition of Done / revert 後の状態検証 / Codex review request の必須項目化。
- **`a4_2 spec.js` の `expectNoOverflow` ローカル実装と `layout-assertions.js` の `expectNoHorizontalOverflow` 統合**: 別 PR 候補。重複解消で保守性向上。
- **ソースリファクタリング**: 別 Sprint。CORE / TEMP / POC / LEGACY 分類前提（shogi_v4.html の機能別分割 or 分類タグ付け方針）。
- **マニュアル更新**: 過去参加者パネル 3 セクション分離 / ボタン色強調復活 / F7 last_class 2 択化（A-4.5 / A-4.6）の利用者向けマニュアル。次セッション。
- **他 5 プロジェクト展開**: golf-compe / bp-matching / x-support / file-organizer / shogi-coach への DevSecOps v2.0 展開（UX/Operational/Assumption Safety を先行適用）。**現状は DevSecOps v2.0 凍結中のため保留**。

### 受付業務まわり

#### QRコード受付
参加者が自分のスマホでQRを読んで入力する方式。幹事の入力ゼロになるが、参加者側に操作を求める。月例大会の参加者層（年配の方も多い）を考えると全員対応は現実的でない。

**見送り理由**：参加者層への依存度が高く即効性が低い。マスタ活用の完成を優先する。

#### タッチパネル受付
**見送り理由**：QRより導入コストが高く、同じ課題を持つ。

---

## 永続見送り事項

- ローマ字検索
- ふりがな自動推定
- 異体字自動統合
- 半角カナ対応
- iOS Safari 印刷警告対応（A-5、2026-05-08 スキップ判断、印刷機能不要のため永続スキップ）

---

## 判断経緯ログ（A-4 系 + 基盤強化フェーズ）

直近の重要な判断・revert 経緯を時系列で記録（Postmortem v2 の補完、ロードマップ単独で経緯を追える状態を維持）。

### 2026-05-07: A-4.2.1 hotfix の的外れ revert
- A-4.2 リグレッションへの hotfix として `pp-add-btn` を `mousedown` + `touchstart` 化（PR #19）
- 髙橋さん実機 Web Inspector 観察で「event handler は元々呼ばれていた」と判明 → handler 戦略変更は的外れ
- PR #20 で revert（commit `be270cb`）。「効かない修正を main に残さない」原則
- 真因は UX 仕様（既登録者のクラス変更手段がない）→ A-4.3 で根因対応

### 2026-05-07: A-4.4 失敗 → revert（P0 障害）
- 過去参加者パネル 2 セクション分離 + 行内「現在:Xクラス」テキスト追加（PR #22）
- Codex Gate Review A 判定 + e2e 517 緑で main マージ（commit `0ad3099`）
- iPhone 375px 実機で行内要素圧迫 → 氏名 span が縦書き化する P0 障害発覚
- 即 revert（commit `a66f418`）。Codex Devil's Advocate §1「iOS Safari 固有の罠」を実機が外れた典型例
- 真因: `chromium-desktop` project の `setViewportSize` では iOS Safari 特有 rendering を再現できない構造的問題

### 2026-05-07: A-4.5 で構造的再発防止
- 3 セクション分離（Aクラス済 / Bクラス済 / 未エントリー）+ 行 2 段レイアウト（PR #23、commit `cf7a970`）
- 「現在:Xクラス」テキスト追加を断念し、視覚言語をセクション位置に一本化
- 行 2 段化（1 段目=氏名+A/B、2 段目=ふりがな+前回:Xクラス+日付）で氏名 span に十分な幅確保
- **mobile-375 project の visual regression を新設**（`visual_regression_mobile.spec.js`、playwright.config.js で testIgnore 分離）→ 同種の P0 障害を構造的に検出可能に

### 2026-05-07: A-4.6 で色強調を「ボタン style のみ変更」で復活
- A-4.5 マージ後の実機テストで「タップしたボタンが分かりにくい」指摘
- A-4.4 の轍を踏まないため**行構造・行内要素は A-4.5 と完全同一**、ボタンの class/style/text 属性値のみ変更（PR #24、commit `d5f28ff`）
- A 済→#bbdefb / B 済→#ffe0b2 の active 色強調復活、`pp-add-btn-highlight`（前回参加クラス強調）は撤廃のまま維持

### 2026-05-08: DevSecOps v2.0 凍結判断
- DevSecOps v2.0 Slim v1.1 配置済（commit `ebe6675`、ChatGPT Full Review B 判定 Must Fix 5 + Should Fix 5 反映）
- Full 版作成・他 5 プロジェクト展開は**当面凍結**
- 凍結理由:
  - 仕様書肥大化リスク（Slim 段階で 401 行、Full 化でさらに膨張）
  - ChatGPT 警告（過剰な仕様化が運用負荷を生む可能性）
  - 髙橋さん本来ミッション（shogi 運営 / note 執筆 / 書籍 / コーチング）からの乖離回避
- Slim v1.1 で凍結 → 必要時に「shogi で実装した既知の対策（Layout Safety L-1〜L-4）」を Slim に追記する形で運用

### 2026-05-08: A-5 スキップ判断
- A-5「iOS Safari 印刷警告対応」を**永続スキップ**
- 理由: 印刷機能を運用上使わないため

### 2026-05-08: Layout Safety L-3 + L-4 完了
- DevSecOps v2.0 Slim v1.1 で計画した P1 対策のうち L-3 / L-4 を実装（PR #25、commit `43d5f7d`）
- production code **0 行改修**（test/helpers/ + test/e2e/ のみ）
- A-4 系で露呈した「画面レイアウトのチェックもできないテスト」状態を解消
- A-5 系着手前の基盤強化として L-3/L-4 を入れた形

### 2026-05-12: A-5.1-SAVE-DESIGN-001 配置（保存処理棚卸し v0.1）
- MASTER-001（PR #40）で確立した「保存後 re-read で検証」原則を shogi_v4.html 内の他保存処理に **段階的に** 適用するための前提整備（PR #45）
- `docs/notes/20260512_shogi_a5_1_save_design_001_persistence_inventory_v0.md` 配置
- 27 callsite + 6 I/O 系を網羅、verify 軸の違いに応じた段階適用順序を設計
- v0.1 で Codex Must Fix / Should Fix を反映し、SAVE-001 を `removePlayer` 1 件に絞り込む方針確定

### 2026-05-12〜13: A-5.1-SAVE-001 / 002 / 003 完了
- **SAVE-001（PR #46）**: `removePlayer` の保存未確認検知（`verifyPlayerAbsent`）
- **SAVE-002（PR #47、commit `a19e193`）**: `addPlayer` の保存未確認検知（`verifyPlayerPersistedById`、id + cls + name 3 軸）
- **SAVE-003（PR #48、commit `1e13ce1`）**: 大会進行 core path 4 関数（`startTournament` / `generatePairing` / `setWinner` / `submitRound`）の保存未確認検知。新規 helper `readPersistedState`（schema 検証付き state 再読込）+ `pairingsMatchSnapshot`（field-compare）
- 共通方針: 「保存失敗」と断定せず「保存未確認」として `showMsg(.., 'warn')` + `console.warn`。alert / rollback / retry なし。SAVE-DESIGN-001 §1.4 と整合
- スコープ規律: 1 PR を Must スコープに絞り、Should / Nice-to-Have は別 PR に分離する運用が定着。SAVE-002 / SAVE-003 はいずれも v0.1 §5 計画より狭く着地（SAVE-002 = S01 のみ、SAVE-003 = 大会進行 core path に再定義）
- 残タスク候補（SAVE-003b / SAVE-004 / SAVE-UX 他）と実装スコープ差分は `docs/notes/20260513_shogi_a5_1_save_completion_summary_v0.md` に集約

### 2026-05-13: A-5.1-SAVE-004 完了
- **SAVE-004（PR #50、commit `42e4673`）**: `generatePairing(cls)` の保存確認を **length-only → `pairingsMatchSnapshot` による field-compare に強化**。同件数だが中身が古い stale pairings を検知できるようになり、`submitRound` と verify 粒度が揃った
- SAVE-003 Codex 再レビューの Nice-to-Have 指摘に対応する小粒 PR。**既存 `pairingsMatchSnapshot` を流用**、新規 helper は追加していない
- 通知方針は SAVE-001 / 002 / 003 と同じ（`showMsg(.., 'warn')` + `console.warn`、alert / rollback / retry / 警告バナー / warn 集約なし）
- Codex Review: A / Go（Must / Should / Nice-to-Have すべてなし）。Unit / Security / E2E すべて SUCCESS
- 残タスク候補は SAVE-003b / SAVE-UX に縮小

### 2026-05-13: A-5.1-SAVE-003b-1 完了
- **SAVE-003b-1（PR #52、commit `af2f173`）**: 参加者追加経路 4 callsite（`handleSuggestClassAdd` / `handlePastParticipantClassAdd` クラス変更 / 同 新規追加 / `finalizeAddPastParticipants`）に保存未確認検知を追加
- **既存 `verifyPlayerPersistedById(id, cls, name)` を流用**、新規 helper は追加していない
- `handleSuggestClassAdd` は postSuccess に verify 引数（pid / cls / name / warnMsg）を追加し追加・クラス変更経路で共通化
- `handlePastParticipantClassAdd` は新規追加経路 / クラス変更経路の両方に verify ブロック
- `finalizeAddPastParticipants` はバッチ追加分の id/cls/name を `addedTargets` で保持し、**複数件未確認でも UI warn は 1 回に集約**（件数は warn 文言に含める）
- 通知方針は SAVE-001 〜 004 と同じ（`showMsg(.., 'warn')` + `console.warn` のみ、alert / rollback / retry UI / 警告バナー / warn 集約 UI / 文言短縮 / debounce なし）
- `console.warn` タグは `SAVE-003b` で統一
- Codex Review: A / Go（Must / Should なし、Nice-to-Have は後続対応のみ）。Unit / Security / E2E すべて SUCCESS
- 次候補: SAVE-003b-2（対局画面編集系）/ SAVE-003b-3（手動編集系、SAVE-UX 集約方針合意が前提）/ SAVE-UX

### 2026-05-13: A-5.1-SAVE-003b-2 完了
- **SAVE-003b-2（PR #54、commit `9f4144f`）**: 対局画面編集経路 3 callsite（`bindChangePairingModalEvents` 対戦相手変更 / swap、`bindEditPastResultModalEvents` p1 / p2）に保存未確認検知を追加
- `bindChangePairingModalEvents` は `save()` 後に `readPersistedState()` + `pairingsMatchSnapshot(persisted.pairings[cls], state.pairings[cls])` で配列全体の field-compare。単発変更 / swap の両経路で同じロジック。swap 後 idx=1 だけ stale な状態も検知可能
- `bindEditPastResultModalEvents` は p1 / p2 共通の **ローカルクロージャ `verifyPastResultPersisted_ep(expectedWinner)`** で `results[cls][roundIdx][matchIdx].winner` をインライン確認。多次元 index 欠落（roundIdx / matchIdx）も検知
- **既存 `readPersistedState` / `pairingsMatchSnapshot` を流用**、トップレベル新 helper は追加していない
- 通知方針は SAVE-001 〜 004 / SAVE-003b-1 と同じ（`showMsg(.., 'warn')` + `console.warn` のみ、alert / rollback / retry UI / 警告バナー / warn 集約 UI / 文言短縮 / debounce なし）
- Codex Review: A / Go（Must / Should なし、Nice-to-Have は後続対応のみ）。Unit / Security / E2E すべて SUCCESS
- 同時期に Mini Shai-Hulud 第二波の影響確認を実施: SHOGI-TOUR repo / 開発端末 / GitHub Actions すべて影響なし。**本 PR には Mini Shai-Hulud 関連の修正は混入していない**
- 次候補: SAVE-003b-3（手動編集系、SAVE-UX 集約方針合意が前提）/ SAVE-UX / Security hygiene（`trufflesecurity/trufflehog@main` を SHA / tag 固定に変更、Mini Shai-Hulud 影響確認時に発見した別件、SAVE 系本流とは別 PR で扱う）

---

## 受付業務の構造（2026-05-05 整理）

| # | 作業 | 担当 | 自動化可否 |
|---|------|------|-----------|
| ① | カード記入依頼（名前・クラス・支部員・中学生以下） | 参加者自身 | 不要 |
| ② | 情報をアプリに登録 | 幹事 | ★マスタで軽減 |
| ③ | 集金 | 幹事 | 人手必要 |
| ④ | 1局目手合い生成 | アプリ自動 | 完了済み |

---

## 更新ルール

- 議論のたびにこのファイルを更新してGitHub `shogi/docs/specs/` にコミットする
- 「見送り」「こちらに決定」などの判断経緯も必ず記録する
- Claude.aiはロードマップ更新時にgitコマンドをセットで出力する
