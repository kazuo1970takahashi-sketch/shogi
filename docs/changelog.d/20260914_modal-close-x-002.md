## MODAL-CLOSE-X-002: バックアップ画面の「閉じる」が初期表示で見えない

- **問題**: 「バックアップ」画面は本文が画面の 90% を超えるとスクロールする作りで、「閉じる」は一番下だけ。iPhone では初期表示で隠れ、閉じ方が分からない（背景タップでも閉じない）。2026-09-14 作者報告。#982（ヘルプ画面）は取り違えで、作者が指していたのはこの画面。
- **修正**: 見出し「バックアップ」の右端に「✕ 閉じる」を常設（下の「閉じる」は残す＝両端から閉じられる）。表示のみの変更で保存・復元のロジックは無改変。
- **同梱**: #982 Codex P2 の解消＝「✕ 閉じる」の色指定を inline から class（btn-outline-primary）へ（ヘルプ・バックアップ両方）。
- テスト: test/test_modal_close_x_002.js 新設（11 件・変異 3 件で赤を確認）
