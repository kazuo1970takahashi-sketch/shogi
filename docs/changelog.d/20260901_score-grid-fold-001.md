## SCORE-GRID-FOLD-001: 対局管理の「暫定成績」を畳む

- **決定**: 2026-08-24 に作者が「畳む」を承認済み（削り7件の1件目）。
- **畳んでよい根拠**: 暫定成績は**最終結果タブと星取表にも同じものがある**＝対局管理では参照系。
  #661（対戦済みリスト）が同じ理由で既に畳まれており、**その型をそのまま移植した**。
- **修正**: `buildScoreGridHtml` の出力を `details` に格納。モバイル（幅600px未満）は既定で畳み、
  PC は `open` で従来表示。判定は `buildPlayedHistoryHtml`（#661）と**同じ流儀**（`isSP` 引数・
  省略時は `window.innerWidth` で判定＝テスト注入可）。開閉は **native disclosure**＝JS/bind を足さない。
  見出しの `h3` は `summary` が引き継ぐ（**見出しが二重にならない**ようにする）。
- **効き**: **−2,110px**（30名2クラス・375px の実測）。進行に関係なく一定。
- **★ 累計（このスライス群の到達点）**: `10a052b` → 本 PR。

  | 進行 | いま | #941〜#944 | |
  |---|---|---|---|
  | 0% | 12,905px | **9,689px** | −24.9% |
  | 50% | 13,257px | **7,332px** | −44.7% |
  | 100% | 13,565px | **5,180px** | **−61.8%** |

  ★ 出発点は「進むほど長くなる」（12,905 → 13,565px）。到達点は**進むほど短くなる**（9,689 → 5,180px）。
- **不変**: 中身（`.score-grid` の各カード＝番号・氏名・勝数・勝敗数）は**1文字も変えていない**。
  順位計算・並び順・データ層は無改変。`renderTournament` の組立順（現在の組み合わせ＜暫定成績）も不変。
- テスト: `test/test_score_grid_fold_944.js`（21件）。#661 の受入テストと同じ型で、
  **構造を読むだけでなく純関数として実行**する（`vm` サンドボックスで `isSP` を振る）。
  A: details・summary・`open` の条件化・**h3 を出さない**・**開閉の JS を足していない**・`.score-grid` 存置。
  B: 組立順（#661 C1 の不変条件）。C: `isSP` true/false/省略×幅500/省略×幅1024 の4通りを実行して
  開閉を確かめる＋`sorted`/`wins` を書き換えない。★D: **中身が畳む前後で同一**であること
  （開閉だけの違いであること）と、4要素が従来どおり出ること。
  変異5本すべてで赤を実測（details をやめる／モバイルでも open／window 幅の既定判定をやめる／
  **中身の勝敗数を落とす**／h3 を残して見出しを二重にする）。
