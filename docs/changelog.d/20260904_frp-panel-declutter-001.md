## FRP-PANEL-DECLUTTER-001 (#957): 「1局目 未割当参加者」パネルの説明文3本をヘルプに一本化

- **問題**: 作者の実機確認（v153）で、対局管理の部分開始パネルが「ごちゃごちゃしている原因」。説明文3本（受付順で2人ずつ／部分開始中の説明／2人以上を選択）は1回理解したらもう読まないもので、375px・未割当2名でパネル 560px のうち 266px（48%）を占めていた。
- **修正**: 3本を消す（`buildFirstRoundPartialSectionHtml`）。残すのは 見出し＋「？ ヘルプ」／「N名を選ぶと M組」の1行／ボタン2つ／チェック一覧。同じ内容は `HELP_TEXTS['first-round']` に全部あり、「2人以上」は押したときの `showMsg` で出る。パネルは 274px。
- **Codex で直したこと**: 「2人以上を選択してください」は `showMsg` → `#reg-msg`（受付ペインの中）で、対局管理タブでは元から見えていなかった。止めた理由（N5）として、発生元のパネル内に `role="alert"`・danger 面色のスロット（`frpErr_<cls>`・`.frp-err`・既定 hidden＝高さゼロ）を置き、見出し語「⚠ 作成できません」と次の行動（チェックを2つ以上つけてもう一度押す）を添える。チェックを触ると消える。`showMsg` は履歴として残す。ヘルプの「まとめて作成」に組成順序「受付順（番号順）に隣どうし2人ずつ」を残した。
- **やらないこと**: 選択して作る導線を畳む（案B）。9/12 の実運用で「選ぶ」の頻度を見てから決める。
- テスト: `test/e2e/frp_panel_declutter_957.e2e.js`（実 Chromium・375px・実寸／残す要素／ヘルプ本文／showMsg）。`test_frp_impl_003.js` BIND3 は「文が在る」から「p は projection の1本だけ」へ。
